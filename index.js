/**
 * @description
 * Gets an array of numbers or objects with name,
 * will multiply the numbers or capitalize the object name
 * @param {(number|{name: string})[]} arr
 * @return {(number|{name: string})[]}
 */
function multiplyOrCapitalize(arr) {}
